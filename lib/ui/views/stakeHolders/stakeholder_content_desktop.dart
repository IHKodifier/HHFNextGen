import 'package:flutter/material.dart';

class StakeHolderContentDesktop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Material(
      child: Container(
        color: Colors.purple[100],
        child: Center(child: Text('StakeHolderContentDesktop')),
      ),
    );
  }
}

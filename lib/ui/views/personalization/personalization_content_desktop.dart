import 'package:flutter/material.dart';

class PersonalizationContentDesktop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.purple[300],
      child: Text('PersonalozationContentDsktop'),
    );
  }
}
